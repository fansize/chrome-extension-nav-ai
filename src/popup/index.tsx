import "../main.css"
import ToolGrid from "../components/tool-grid"

const Popup = () => {
  return (
    <div
      style={{
        width: 400,
        padding: 6,
      }}>

      <ToolGrid />
    </div>
  )
}

export default Popup
